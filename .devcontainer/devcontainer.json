{
    "name": "Elixir",
    "build": {
        "dockerfile": "Dockerfile",
        "args": {
            "VARIANT": "latest"
        }
    },
    "settings": {
        "terminal.integrated.shell.linux": "/bin/zsh"
    },
    "extensions": [],
    "postCreateCommand": "mix deps.get",
    "remoteUser": "vscode"
}